class User:
    def __init__(self, type):
        self.type = type

    def hasSpotifyAccount(self):
        # Doesn't matter what it does for now
        return False

    def isGuest(self):
        # Doesn't matter what it does for now
        return False